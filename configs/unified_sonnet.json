{
 "config_id": "unified_sonnet",
 "description": "A15 policy-robust candidate (sonnet arm): frozen toolset, draft-first/repair-from-failure prompting \u2014 submit the complete proof in ONE step call (commit-good-prefix keeps everything up to the first failure), then repair from the failure point with try/auto_close.",
 "server": {
  "command": "{repo}/_build/default/src/session_server/rocq_agent_session.exe",
  "args": [],
  "env": {
   "ROCQ_STEP_TIMEOUT": "10",
   "ROCQ_QED_TIMEOUT": "60",
   "ROCQ_TRY_TIMEOUT": "5",
   "ROCQ_ENABLE_TOOLS": "step,rollback,state,try,auto_close",
   "ROCQ_HINTS": "1",
   "ROCQ_SUGGEST": "1",
   "ROCQ_ENV_V2": "1",
   "ROCQ_AUTO2": "0",
   "ROCQ_EXEMPLARS": "0"
  }
 },
 "mcp_server_name": "rocq",
 "allowed_tools": [
  "mcp__rocq__step",
  "mcp__rocq__rollback",
  "mcp__rocq__state",
  "mcp__rocq__try",
  "mcp__rocq__auto_close"
 ],
 "model": "claude-sonnet-5",
 "max_turns": 30,
 "attempt_timeout_s": 300,
 "system_prompt": "You are an expert Rocq (Coq) prover (Rocq 9.1; the file's imports are loaded, and the standard tactic modules Lia/Lra/Psatz are ALWAYS preloaded \u2014 lia, nia, lra, nra, psatz work everywhere. Never use Require). You drive a LIVE prover session where the theorem statement has already been executed: the goal is open.\n\nSTRATEGY \u2014 draft first, repair from the failure point:\n1. Write your best COMPLETE proof and submit it as ONE step call (step accepts many sentences: `Proof. ... Qed.`). Every sentence up to the first failure is committed permanently; if it all passes you are done in one move.\n2. If a sentence fails, you are left standing exactly there, with the error, hints, and the live goal. Repair locally: auto_close{} (runs the standard finisher portfolio), try{candidates} for several ideas at once, step to commit. Then continue or re-draft the remainder as another single step call.\n3. rollback{count} undoes committed sentences; state{} re-renders goals.\n\nStrict rules: never use admit, Admitted, Axiom, Parameter, Hypothesis, Variable, or Require \u2014 an external checker rejects them. Finish with `Qed.` When a tool reports PROOF COMPLETE, reply exactly DONE. If you cannot finish, reply exactly GIVEUP.",
 "task_prompt_template": "Prove this theorem (already loaded in the session; its goal is open):\n\n```coq\n{prefix}```\n\nDraft the complete proof and submit it as one step call; repair from the failure point if needed."
}